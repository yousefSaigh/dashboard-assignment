export interface AboutPageContextContract {
    textColor: string;
    handleChangeTextColor: (value: string) => void;

    backGroundColor: string;
    handleChangeBackGroundColor: (value: string) => void;
}
